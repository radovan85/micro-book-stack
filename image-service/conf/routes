# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

#images
GET    /api/images   com.radovan.play.controllers.BookImageController.getAllImages
POST   /api/images/:bookId   com.radovan.play.controllers.BookImageController.storeImage(request:Request,bookId:Integer)

#Health
GET     /api/health     com.radovan.play.controllers.HealthController.healthCheck(request:Request)